package control;

import control.BodyState;
import nape.geom.Vec2;
import nape.phys.Body;
import nape.phys.BodyType;

class PhysicsBody {

	/*public var id(default, null):String;
	
	public function new(id:String, type:BodyType=null, position:Vec2=null) {
		super(type, position);
		this.id = id;
	}
	
	public function getState():BodyState {
		return {
			positionX: position.x,
			positionY: position.y,
			velocityX: velocity.x,
			velocityY: velocity.y,
			angularVel: angularVel
		}
	}
	
	public function applyState(state:BodyState):Void {
		position.x = state.positionX;
		position.y = state.positionY;
		velocity.x = state.velocityX;
		velocity.y = state.velocityY;
		angularVel = state.angularVel;
	}*/
	
}